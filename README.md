# 👕 Склад одежды — Информационная система (Вариант 19)

**Полноценная веб-реализация лабораторной работы по MS Access**
Стек: **FastAPI + PostgreSQL + Docker**
Тёмная тема (3 цвета) | Реализация СППР по пороговому методу

---

## 📋 Полное соответствие методическим требованиям

| Требование                          | Реализация                                                                      |
| ----------------------------------- | ------------------------------------------------------------------------------- |
| Вариант 19 — Склад одежды           | ✅ Модели: `suppliers`, `products`, `characteristics`, `product_characteristics` |
| 6+ поставщиков                      | ✅ 7 поставщиков                                                                 |
| 4+ продукции у каждого              | ✅ 30 товаров (у каждого поставщика минимум 5)                                   |
| 5+ характеристик                    | ✅ 10 характеристик качества                                                     |
| Нормы (min/max) и реальные значения | ✅ Таблица `product_characteristics` (500+ записей)                              |
| СОД — таблицы, формы, запросы       | ✅ CRUD-интерфейс, пагинация, SQL-конструктор                                    |
| ИСУ — отчёты, сортировка, агрегация | ✅ Фильтрация, экспорт Excel/JSON                                                |
| СППР — пороговый метод              | ✅ Полная реализация алгоритма из методички                                      |
| Форма с выпадающими списками        | ✅ Выбор поставщика и продукции                                                  |
| Отчёт с фильтром                    | ✅ Детальный отчёт по качеству                                                   |
| Обучение СППР                       | ✅ Автоподбор Δx и сдвиг порога                                                  |
| Бэкап / восстановление              | ✅ `pg_dump` / `pg_restore`                                                      |
| Архивация таблиц                    | ✅ Backup + Excel + JSON + Drop                                                  |
| Тёмная тема (3 цвета)               | ✅ Чёрный (#0a0c0e), синий (#1e2b3a), золото (#c7a97e)                           |

---

## 🚀 Команды для запуска и перезапуска

### 1️⃣ Первый запуск (чистая установка)

```bash
git clone https://github.com/your-repo/clothing-warehouse.git
cd clothing-warehouse

docker-compose up --build
```

---

### 2️⃣ Полный перезапуск (сброс БД + пересборка)

```bash
# Остановить контейнеры и удалить все данные (включая тома)
docker-compose down -v

# Удалить старые образы (опционально)
docker system prune -a -f

# Пересобрать проект с нуля
docker-compose up --build
```

---

### 3️⃣ Быстрый рестарт (без сброса данных)

```bash
# Перезапуск контейнеров
docker-compose restart

# Или остановить и запустить заново
docker-compose down
docker-compose up -d
```

---

### 4️⃣ Просмотр логов

```bash
# Все логи
docker-compose logs -f

# Логи только приложения
docker-compose logs -f app

# Логи только базы данных
docker-compose logs -f postgres
```

---

## 🧠 Архитектура системы

* Backend: FastAPI
* База данных: PostgreSQL
* Контейнеризация: Docker + Docker Compose
* Экспорт данных: Excel / JSON
* СППР: Пороговый метод диагностики качества продукции

---

## 🎨 Дизайн

Тёмная минималистичная тема:

* Основной фон: #0a0c0e
* Вторичный цвет: #1e2b3a
* Акцент: #c7a97e

---

Проект полностью соответствует требованиям лабораторной работы и реализует функциональность СОД, ИСУ и СППР в веб-формате.
